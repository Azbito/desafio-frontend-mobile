import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  divider: {
    borderBottomWidth: 1,
    borderBottomColor: 'gray',
    marginVertical: 10,
    width: '100%',
  },
});
